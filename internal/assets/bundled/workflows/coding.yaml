name: coding
description: |
  Drives software engineering work from Jira ticket to reviewed code, coordinated
  hierarchically by the coding-orchestrator. The orchestrator routes each request to a goal
  (requirements-only, plan-only, implement-only, review-only, full-cycle, or session-ops) and
  runs only the capabilities that goal needs — so it can fetch requirements alone, or drive
  the full cycle end-to-end.
  Capabilities:
  - Context gathering — jira-requirements-fetcher and pr-details-fetcher run in parallel.
  - Session bootstrap — session-bootstraper provisions a git worktree/feature branch and
    registers the session in sessions/registry.json.
  - Planning — implementation-plan-writer produces generated-docs/<JIRA_KEY>/implementation-plan.md
    (frontmatter status: draft); a human approves before any code is written.
  - Implementation loop — code-implementer executes the approved plan; code-reviewer gates on
    a machine-readable verdict in generated-docs/<JIRA_KEY>/review-summary.md; the loop
    repeats until approved.
  - Session lifecycle — session-manager lists and cleans up parallel worktrees.
  Progress is tracked on disk in generated-docs/<JIRA_KEY>/state.yaml (session-state skill),
  so interrupted sessions resume from the recorded phase instead of restarting. Missing
  prerequisites are resolved dynamically: from disk first, then by delegating to the producing
  agent, and only then by asking the user.
agents:
  - coding-orchestrator
  - jira-requirements-fetcher
  - pr-details-fetcher
  - session-bootstraper
  - implementation-plan-writer
  - code-implementer
  - code-reviewer
  - session-manager
skills:
  - code-review-documentation
  - implementation-plan
  - session-state
  - worktree-cleanup
  - worktree-setup
